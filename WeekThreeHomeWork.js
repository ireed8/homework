// param1 & param2 are parameters
function myFunction(ham, cheese) {
    // printing each parameter
    console.log(ham);
    console.log(cheese);
  }
  // 1 & 2 are arguments passed into the function
  myFunction(1, 2);
  // prints:
  // 1
  // 2